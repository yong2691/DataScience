06-03 목요일
SQL 교육 이미향 강사님

<<<지난 수업 리뷰>>>

<<<수업 시작>>>
w3schools.com
들어가서 SQL 튜토리얼

SQL is a standard language for storing, manipulationg and retrieving data in databases.
-> DB에서 자료를 저장, 조작, 검색

DB를 그림으로 설명하면,
예시 ) 
학생관리 시스템
	학번	이름	국어	영어	수학 << field = item 이라고 한다.
            --------------------------------------------------
오퍼레이터에 의해서 값(데이터=점수)을 넣으면 된다.

Table 안에는 column과 row가 있다.

이러한 Table들을 DataBase 라고 한다.
DB에 자료를 저장, 조작, 검색 한다.

Our SQL tutorial will teach you how to use SQL in:
	mySQL(돌고래 로고), SQL Server, MS Access, Oracle, Sybase, Informix, Postgres, and other database systems.
mySQL을 오라클사에 팔아 넘기고 mySQL만든 사람들이 마리아DB도 만들었따.(물개 로고)
근데 오라클에서 유료로 돌리려고하니까 이제는 무료인 또 마리아DB도 많이 사용한다.

근데 아직은 Oracle을 가장 많이 사용하여, 우리는 Oracle을 배울 것이다.
MS Access는 MS사에서 만든 개인용 DB이다. 

S Q L = Structured Query Language

미국 표준 협회 = American National Standards Institue = ANSI
국제 표준 기구 = International Organization for Standardization = ISO

설치방법---------------------------------------------------------------------------------------------

Developer - resource center - downloads - All Java Download - Database 18c Express Edition (무료버젼)
- 밑에 Prror Release Archive(무료버젼) - Oracle Database 11gR2 Express Edition for Windows x64
- setup.exe - C:\oraclexe 에 깔린다 - 비번은 ora11 -설치 완료
C:\oraclexe\app\oracle\product\11.2.0\server\bin 들어가서 - sqlplus.exe 들어가라

실행방법---------------------------------------------------------------------------------------------

Oracle DBMS 실행 방법01
	- pwd
	  sqlplus 계정
	  pwd : ora11

cmd - sqlplus - 들어가서, 유저네임 입력하라고 나오면? /as sysdba 입력 - 비번은? ora11

GUI : CLI를 이미지화 시켜놓은 것이다.
장점 : 편하다. 마우스 우클릭만 하고 버튼만 누르면 된다.
단점 : 

CLI 명령줄 인터페이스

SQL은 RDB에서
데이트베이스에 질의, 수정, 삭제 등의 작업을 하는 언어의 표준으로 채택되어

어떤 제품이든지 약간의 문법적 차이를 제외하고는 대동소이하며,

따라서, 오라클에서 사용하는 문법과 MS-SQL의 문법은 큰 차이가 없다.
왜냐면 ANSI에 등록되어 표준화가 되어 있기 때문에. 문법이 거의 비슷하다.

2. SQL 문장 종류
1) DQL : Data Query Language (질의어)
SELECT
2) DML : Data Manipulation Language (데이터 조작어)
INSERT, UPDATA, DELETE
3) DDL : Data Definition Language (데이터 정의어)
CREATE, ALTER, DROP, RENAME, TRUNCATE
4) TCL : 
5) DCL : 

명령어 ==================================================

DBMS : Oracle
	실행 01: cmd - sqlplus 입력 - ora11 입력
	실행 02: cmd - sqlplus system/ora11 입력

exit 치면 빠져 나온다.
막 사용하다보면, 내가 어디에 있는지 모른다. 어느 계정에 있는지 모를때,
현재 접속 계정 확인 : show user
* 관리자계정 : sys, system
* 테이블
	- user table
	- 관리용

SELECT column1, column2, ...
FROM table_name;

SELECT     *       FROM table_name;
         컬럼자리           테이블자리

select * from tab; : 테이블의 갯수와 종류를 보여줌
cl scr : 화면 깨끗하게 지움

show pagesize = show pages : pagesize 14 뜬다.
set pagesize 40 : page size가 40으로 바뀐다.

show linesize
set linsize 200 : line size가 200으로 바뀐다.


conn as sysdba 치면, 다시 유저네임 입력하라고 나온다. 사용자를 바꾸는 명령어
	근데 아무계정이나 치고 아무비번이나 쳐도 SYS 라는 계정으로 들어가진다.
conn system 이라고 치면, system이라는 계정의 비번을 입력하라고 한다. ora11을 치면 system으로 접속된다.
conn system/ora11 이라고 치면, 아이디 비번 한번에 치고 들어가는 것이다.















